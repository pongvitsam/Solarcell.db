{
  "name": "solarsave-google-apps-script",
  "private": true,
  "description": "SolarSave backend — push with: npm run push (after clasp login)",
  "scripts": {
    "push": "clasp push --force",
    "pull": "clasp pull",
    "open": "clasp open"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2"
  }
}
